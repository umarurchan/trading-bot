import { UserAlias } from "../../types";
import { ChainClient, ChainUser } from "../generic";
export interface ChainUserAPI {
    privateKey: string;
    identityKey: UserAlias;
    publicKey: string;
    ethAddress: string;
}
export declare function buildChainUserAPI(user: ChainUser): (c: ChainClient) => ChainUserAPI;
//# sourceMappingURL=ChainUserAPI.d.ts.map